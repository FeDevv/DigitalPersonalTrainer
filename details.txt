RESIOCONTO SESSIONE REFACTORING DPT - 24/04/2026

1. STATO DEL REFACTORING:
   - BOOT: Completato. Rinominato in BootLogicController. Implementa BaseCLI.
   - LOGIN: Completato e Blindato. Struttura: LogicController -> UI Interface <- Factory.
   - CONNECTION: Completato. DBConnectionManager silenziato, rimosso System.out.
   - EXCEPTION: Validato. Gerarchia basata su RuntimeException (DPTException).
   - SHARED: Creati sottopacchetti 'ui' (classi base) e 'auth' (Role enum).
   - USERS: Nuova root per i moduli utente.

2. STANDARD ARCHITETTURALI APPLICATI:
   - DUAL-CONTROLLER: Separazione tra logica decisionale e gestione I/O.
   - AGNOSTICISMO UI: Il LogicController parla solo con un'interfaccia (es. LoginUI).
   - LOCAL FACTORY: Ogni pacchetto ha una factory interna per scegliere tra CLI e GUI.
   - DTO (DATA TRANSFER OBJECT): Uso di Java Records per inviare pacchetti di dati completi tra View, Controller e DAO (es. UserCredentials).
   - DRY UI: BaseCLIView e BaseCLIController centralizzano stampe generiche e acquisizione input (readInt, readString).
   - FAIL-FAST: Validazione immediata nei costruttori dei record.
   - ENCAPSULATION: Mantenimento dei DAO e delle implementazioni UI concrete "package-private" ove possibile.

3. PROSSIMI PASSI (DOMANI):
   - Modulo PROPRIETARIO:
     * Creazione DTO per Macchinario, Esercizio, PT, Cliente.
     * Implementazione ProprietarioUI (Interface) e Factory.
     * Refactoring DAO per accettare DTO completi.
     * Allineamento CLIController e View alle classi Base.
   - Modulo SHARED.CATALOGO: Revisione per uniformità.
   - ORCHESTRATOR: Revisione finale per il routing basato sui nuovi LogicController.

To resume this session: gemini --resume 1936f014-1456-48ba-988b-33e86458e8ee