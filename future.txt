1)  aggiungere al boot la modalità CLI color, dove creiamo un'altri elementi in shared.ui che sono
    identici a quelli ora presenti, ma con i colori per CLI, l'orchestrator (o il boot) semplicemente
    andrà ad istanziare il blocco corretto

2)  implementazione di una GUI

3)  Introduzione di BaseDAO<T>: Astrazione della logica JDBC tramite classe base generica.
    Utilizzo di metodi template (executeQuery, executeUpdate) e varargs (Object... params)
    per eliminare il boilerplate nei DAO concreti, centralizzando la gestione delle eccezioni
    e delle risorse JDBC, lasciando ai figli solo il mapping (mapResultSetToEntity).

